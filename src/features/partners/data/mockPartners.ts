import type { Partner } from '../types/partner.types';

/**
 * @deprecated Mock data has been removed in favor of live API integration.
 * Refer to `usePartners` hook and `partnerApiService` for live data.
 */
export const initialPartners: Partner[] = [];
